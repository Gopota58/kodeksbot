# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
venv/
.venv/

# Secrets
.env
# Резервные копии .env (cp .env .env.bak-$(date +%Y%m%d-%H%M%S) перед правками) —
# внутри живые API-ключи, в репозиторий попадать не должны.
.env.bak*

# Agent / local working notes (не для публичного репо — содержит токен бота)
MEMORY.md
_check.py
_vmtest*.py
_diag*.py

# Служебные данные AI-агента (память воркспейса) — не публикуем
.workbuddy-ai/

# Generated / large data (recreate locally)
chroma_db/
models/
evaluation/results.json

# Caches линтера/тестов
.ruff_cache/

# Build artifacts
dist/
build/

# Runtime logs and pid-files
logs/
*.log


# Editor / OS
.idea/
.vscode/
.DS_Store

# Corpus (large binary PDF/DOCX — recreate locally / store separately)
data/docs/
